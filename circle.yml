machine:
  java:
    version: openjdk8
  environment:
    TERM: "dumb"

test:
  override:
    - ./gradlew clean checkstyle check jacocoTestReport
  post:
    - cp -R build/reports/* $CIRCLE_ARTIFACTS
